# Движок расчёта стоимости

Файл: `rental_calculator.py`

## Обзор

Калькулятор вычисляет стоимость аренды теплохода на основе:
- Даты мероприятия (определяет сезон)
- Дня недели (будни/выходные — разные тарифы)
- Временных интервалов (разная цена днём/ночью)
- Фаз мероприятия (подготовка и разгрузка — 50% от тарифа)

## Входные данные

Менеджер вводит 3 строки:

```
16.08.26                    ← дата (DD.MM.YY)
Шустрый бобер              ← название теплохода
16-17-23-23:30             ← времена
```

### Форматы времени

**Полный формат (4 времени):**
```
ЧЧ:ММ-ЧЧ:ММ-ЧЧ:ММ-ЧЧ:ММ
  │      │      │      │
  │      │      │      └─ окончание разгрузки
  │      │      └──────── высадка (конец аренды)
  │      └─────────────── посадка (начало аренды)
  └────────────────────── начало подготовки
```

**Короткий формат (2 времени):**
```
ЧЧ:ММ-ЧЧ:ММ
  │      │
  │      └─ высадка
  └──────── посадка
```
В коротком формате подготовка = `prep_hours` до посадки, разгрузка = `unload_hours` после высадки (берутся из БД теплохода).

## Алгоритм расчёта

### 1. Парсинг (`parse_request`)

```python
date_obj = datetime(2026, 8, 16)  # из "16.08.26"
boat_name = "Шустрый бобер"
times = [time(16,0), time(17,0), time(23,0), time(23,30)]
```

### 2. Получение данных теплохода

```python
boat = get_boat_by_name("Шустрый бобер")
# → { dock: "Мартынова 21 ж", cleaning_cost: 5000, prep_hours: 1.0, ... }

schedule = get_pricing_schedule_db("Шустрый бобер", date(2026, 8, 16))
# → [(16:00, 22:00, 52500), (22:00, 06:00, 63000)]
```

### 3. Сегментация

Мероприятие разбивается на 3 сегмента с разными коэффициентами:

| Сегмент | Период | Коэффициент | Пояснение |
|---------|--------|-------------|-----------|
| Подготовка | prep_start → boarding | **0.5** (50%) | Теплоход готовится, но мероприятие не началось |
| Основной | boarding → disembarking | **1.0** (100%) | Полная аренда |
| Разгрузка | disembarking → unloading | **0.5** (50%) | Гости ушли, идёт разгрузка |

### 4. Расчёт стоимости сегмента

Для каждого сегмента:

```
cost = 0
Для каждого тарифного интервала из schedule:
    overlap = пересечение(сегмент, интервал)  # в часах
    cost += overlap * discount * price_per_hour
```

**Пересечение интервалов:**
```
overlap = max(0, min(seg_end, int_end) - max(seg_start, int_start))
```

### 5. Итоговая формула

```
Итого = Подготовка(50%) + Основной(100%) + Разгрузка(50%) + Уборка
```

## Пример расчёта

**Вход:**
```
16.08.26
Шустрый бобер
16-17-23-23:30
```

**Расписание на 16.08.2026 (Вс):**
- 06:00—22:00: 52 500 ₽/ч
- 22:00—06:00: 63 000 ₽/ч

**Расчёт:**

| Сегмент | Период | Часы | Тариф | Коэфф. | Сумма |
|---------|--------|------|-------|--------|-------|
| Подготовка | 16:00-17:00 | 1.0 | 52 500 | x0.5 | 26 250 |
| Основной | 17:00-22:00 | 5.0 | 52 500 | x1.0 | 262 500 |
| Основной | 22:00-23:00 | 1.0 | 63 000 | x1.0 | 63 000 |
| Разгрузка | 23:00-23:30 | 0.5 | 63 000 | x0.5 | 15 750 |
| Уборка | — | — | — | — | 5 000 |
| **Итого** | | | | | **372 500** |

*Примечание: фактический расчёт может отличаться из-за округления часов в формуле вывода.*

## Формат вывода

```
*16.08.26*

*Шустрый бобер* - https://teplohod-restoran.ru/product/shustrui_bober/
16:00 - Подготовка (50%)
17:00 - Посадка
23:00 - Высадка
23:30 - Разгрузка (50%)
Причал: Мартынова 21 ж
Аренда: (52 500₽/ч x 4.50ч) + (63 000₽/ч x 2.25ч) + 5000₽ (уборка) = *383 000*₽
```

`*текст*` — жирный (парсится на фронтенде как `<strong>`).

## Обработка ночных мероприятий

Если время "заходит за полночь" (например 22:00-03:00), калькулятор автоматически переносит конечное время на следующий день:

```python
if t_start >= t_end:
    dt_end += timedelta(days=1)
```

## Ключевые функции

### `parse_request(text)` → (date, name, [times])
Парсит текст запроса. Нормализует:
- "16" → "16:00"
- "16-17-23-23:30" → [16:00, 17:00, 23:00, 23:30]

### `calculate_rental(date, name, times)` → str
Основная функция. Возвращает форматированную строку.

### `calculate_segment_cost_and_hours(start, end, schedule, discount)` → (cost, breakdown, hours)
Считает стоимость одного сегмента с учётом пересечения тарифных интервалов.

### `compute_overlap(seg_start, seg_end, int_start, int_end)` → float (часы)
Пересечение двух временных интервалов.
